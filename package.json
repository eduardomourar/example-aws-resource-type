{
    "name": "community-monitoring-website",
    "version": "0.1.0",
    "description": "AWS custom resource provider named Community::Monitoring::Website.",
    "private": true,
    "main": "dist/handlers.js",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "npx tsc",
        "prepack": "npm run build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "cfn-rpdk": "npm:@org-formation/cfn-rpdk@^0.4.0",
        "class-transformer": "^0.3.1"
    },
    "devDependencies": {
        "@types/node": "^12.0.0",
        "typescript": "^3.8.3"
    },
    "optionalDependencies": {
        "aws-sdk": "^2.656.0"
    }
}
